## 执行步骤
- 确认开发服务器状态（如已运行则直接提供预览链接）
- 如未运行：
  - 使用 `npm install` 安装依赖（已存在 `package-lock.json`）
  - 使用 `npm run dev` 启动 Vite 开发服务器（默认端口 `5173`）
- 提供可点击的本地预览地址

## 相关信息
- 启动脚本：见 `package.json:6-12`（`dev/build/preview`）
- 环境变量：`src/lib/supabase.ts:3-8` 需要 `VITE_SUPABASE_URL` 与 `VITE_SUPABASE_ANON_KEY`

## 预期结果
- 本地地址可访问：`http://localhost:5173/`
- 若端口占用，将自动切换并提示新地址